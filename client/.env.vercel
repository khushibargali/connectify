# API origin used ONLY by the Vercel build (vite build --mode vercel, see client/vercel.json).
# A VITE_API_URL environment variable set in the Vercel dashboard overrides this file.
VITE_API_URL=https://latitude-voip-cartoons-nowhere.trycloudflare.com
